>>>notifications for all new events

weak map and weak set

>>>progress indicator for web worker export function

>>>sync and offline queue

draft save

>>> stress test button - temporary leads

>>> admin or super admin assigns leads to users

>>> fix the deal select options

multiple pocs in Organizations

>>> calls object store should have userId

multiple users in leads

lead activity in db

>>> when new lead is created do not call all the db

>>> export data tenant wise

>>> fix the ui of lead details

>>> calculate score also based on userId and role and tenantId

curl -X POST http://localhost:3000/update -H "Content-Type: application/json" -d '{"message":"Hello from curl!"}'

add email, phone, plan

s3 for storing images

